# Crie um arquivo Python exercicio01.py. Após, crie uma classe Pessoa com os
# seguintes atributos: nome (str), sobrenome (str), idade (int), naturalidade (str),
# nacionalidade (str), data_nascimento (str), numero_id (int) e filiacao (list). Após, crie
# uma função certidao_nascimento() que exiba as informações da classe. Finalmente,
# crie duas cópias (objetos) dessa classe: pessoa1 e pessoa2.
# Obs.: o atributo filiacao é uma lista que deve receber no índice 0 o nome do pai, e no
# índice 1 o nome da mãe.